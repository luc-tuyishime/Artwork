import styled from 'styled-components'

export const IMG = styled.img`
  border-radius: ${(props) => props.theme.pixelsSize.borderRadius} !important;
  height: ${(props) => props.theme.pixelsSize.LogoHeight};
`
